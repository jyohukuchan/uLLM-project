uLLM-projectで使用される用語とその意味について
用語:意味
AQ4_0:既存AQ4 version 0のpublic format identifier。4はおおよそのbit/parameterを表す。
SQ8_0:FP8 E4M3 SQ version 0のpublic format identifier。8はおおよそのbit/parameterを表す。FP8品質の採否候補ではなく正式採用formatとして扱う。
SQ8_0 implementation plan:SQ8_0を正式採用formatとして実装するための計画。artifact、runtime resident loader、direct kernel、backend dispatch、回帰診断、完了条件を扱う。
bpp:bit/parameter
BF16-error:BF16のraw-valueまたは出力を基準にした誤差
decode-tps:decode token/s
prefill-tps:prefill token/s
prefill total input tok/s:測定区間でbatch内の全requestが処理したprompt/input token数の合計をprefill wall timeで割ったthroughput
decode total generated tok/s:測定区間でbatch内の全requestが生成したtoken数の合計をdecode wall timeで割ったthroughput。single requestの体感tok/sとは分けて扱う
end-to-end total tok/s:prompt/input token数とgenerated token数の合計をend-to-end wall timeで割ったthroughput。prefill/decodeの内訳なしではSQ候補の主判断に使わない
logical batch:複数requestを同じbenchmark runで扱うが、内部kernelはrequestごとに順次実行してもよいbatch計測段階。control plane、result schema、latency計測の検証に使う
real batch:複数tokenまたは複数requestを同じkernel、GEMM/GEMV群、またはbatched executorで実行し、GPU利用率を上げるbatch実行段階。SQ候補の性能判断にはこちらを使う
package-batch-throughput-bench:.ullm.d packageに対して複数requestのprefill/decode/end-to-end total throughputとlatencyをJSONで出すullm-engine CLI。初期実装はlogical batchとして扱う
package batch throughput cold accounting:package-batch-throughput-benchのcold prefill logical batch reportで、prefill mode、cached prefix 0、new prefill token数、prefill後context長、推定attention workを保存するschema項目群
prefill layer step summary:package-token-ids-generate系reportのprefill内で、layerごとのtoken-loop実行時間を合計・平均・分位点として記録する診断情報。real batch prefill化の前後比較に使う
device token-loop prefill:prompt tokenごとにembeddingから全layerをdevice-to-deviceで順に通し、layer間のhost readbackを避けるprefill実験経路。real batchではないが、batched/tiled prefillへ進む前のhost境界削減に使う
batch throughput workload manifest:package-batch-throughput-benchをworkload grid、warmup、measured repeat、保存先付きで順次実行するためのJSON入力。結果はinference-benchmark-result-v0.1 JSONLへ変換して保存する
executor parallelism fields:workloadのconcurrent_requestsではなく、実際のexecutorが同時に処理するtoken数・request数・real batch可否を表すbenchmark metadata。logical batch誤読を避けるために使う
cached prefix prefill:既存のKV cache長Lに対して新規input chunk M tokenを追加するprefill系workload。速度の分母はMだが、attention costは主にM*L+M^2/2で決まるため、LとMを分けて記録する
long context upper bound:2^16 token級のcold prefillやcached prefix付きprefillで、常用benchmarkではなく長コンテキスト上限の実行可能性、OOM境界、attention scalingを確認する測定枠
runtime cached prefix attention smoke:synthetic Q/K/Vを使い、既存KV cache長Lに対して新規chunk M tokenを測るruntime component smoke。executorはcached_prefix_chunkedとdecode_loopを選択でき、cached prefix attentionのbaseline、M方向chunk並列化、長prefix時のOOM/速度境界を確認する
cached prefix chunked attention runtime:既存KV cache長Lと新規chunk Mを一度のruntime callで扱うcached prefix attention実行経路。現v0は素朴な1 output element 1 thread実装で、tiled/blocked attentionの前段baselineとして扱う
shared-score cached prefix attention kernel:cached prefix attentionで1 blockを1 token/headに割り当て、max scoreとsoftmax denominatorをvalue次元間で共有するHIP kernel。weighted value側ではまだscore再計算が残るため、tiled attentionの前段改善として扱う
source-shared cached prefix attention kernel:cached prefix attentionで各source timestepのQK dotとsoftmax weightをblock内で1回計算し、value次元のthreadへ共有するHIP kernel。value_dimがblock size以下の現Qwen条件ではweighted value側のscore再計算を避ける
FlashAttention2 cached prefix task:R9700/RDNA4向けにcached prefix prefillとcold prefillのattention componentをtile化し、online softmaxでK/V tileとscoreを再利用する次タスク。RDNA2/V620向けBF16非対応やFP32 dequant fallbackは後続に保留する
source-shared causal attention kernel:causal prefill attentionで1 blockを1 token/headに割り当て、各source timestepのQK dotとsoftmax weightをblock内で1回計算してvalue次元のthreadへ共有するHIP kernel。cold prefillのO(N^2) attention componentを改善するために使う
length-aware RoPE batch guard:self-attention batch smokeのRoPE比較で、固定abs toleranceではなくposition長に応じた上限付きabs floorを使うguard。現capは4e-3。長尺位置でのf32 sin/cos差分を速度評価前の偽失敗として扱わないために使う
sampled causal attention batch guard:self-attention batch smokeの1024 token以上で、full host causal attention referenceを作らず、代表timestep/head/valueだけをhost計算してattention出力を検証するguard。GPU timed runとverification時間を分け、長尺prefill計測を現実的な時間で進めるために使う
self-attention block batch smoke:package-self-attn-block-batch-smokeで、self-attention prefill batch pathのinput RMSNorm、Q/K/V AQ4 batch projection、QK norm/RoPE batch、causal attention、output gate、o projection batch、residual addを同一timed runで測るsmoke。attention-onlyとの差からo projection/residual追加分を分離する
runtime cached prefix attention sweep:tools/run-runtime-cached-prefix-sweep.pyでruntime-cached-prefix-attn-smokeをL/M/executorのgridとして実行し、Phase C4用のJSONLとMarkdown summaryを保存する探索runner
FP8 cached prefix KV cache:runtime-cached-prefix-attn-smokeのcached-prefix prefill経路で、K/V cacheをper-tensor scale付きE4M3 byte列として保存し、kernel内でFP32へ復号してattentionを計算する実験用cache形式
SQ FP8 per-tensor scale layout:SQ FP8 artifactでcandidate全体をmixed scaleとして扱い、各fp8_tensors entryごとにrow/row_block/tensor scaleとrow_block幅を保存するlayout。T2ではk_proj row-block16とup_proj row-block32のような混合scale probeに使う
runtime include split:ullm_runtime.cppを単一translation unitのまま、HIPRTC source stringやC ABI wrapperを役割別.incへ分ける整理。匿名namespace内helperやHIPRTC依存を壊さず、後続の.cpp分割へ進むための中間段階
prefill RMSNorm batch smoke:package prompt embeddingを[tokens,hidden] device bufferに載せ、segmented RMSNormで複数tokenを一括正規化するsmoke。projection/attention/MLPのreal batch化前にprefill buffer境界を検証する
AQ4 matvec batch runtime:同じAQ4 resident matrixに対して複数input vectorを[tokens,cols] layoutで受け取り、[tokens,rows] outputを返すruntime API。prefill projectionをtoken batch化するための最初のAQ4 projection境界
package prefill AQ4 matvec batch smoke:package prompt embedding行を[tokens,hidden] device bufferへ載せ、指定AQ4 projection tensorをAQ4 matvec batch runtimeでまとめて実行し、materialized F32 matvec由来のexpected outputと比較するsmoke。attentionやMLP全体ではなくprojection batch kernelの正しさと速度を測る
package linear attention projection batch smoke:linear attention layerのinput RMSNorm後のhiddenを[tokens,hidden] device bufferとして保持し、in_proj_qkv / in_proj_z / in_proj_a / in_proj_bをAQ4 matvec batch runtimeでまとめて実行するsmoke。recurrent state scanやMLPは含まず、linear attention prefillのprojection batch境界を検証する
package linear attention qkv prepare batch smoke:linear attention layerのinput RMSNorm後にAQ4 qkv projectionをtoken batch実行し、そのdevice上のqkv outputをbatch qkv prepare runtimeへ渡してconv+SiLU、q/k L2 norm、v split、conv history更新を検証するsmoke。qkv projection自体のF32参照比較はpackage linear attention projection batch smokeに分ける
FQ8_0:会話上のtypo。正式なpublic format identifierとしては使わず、SQ8_0を使う。
raw-value:scale・codebookを一切考慮しない各パラメータの値
codebook:LUTによる変換表
codebook-index:codebookを参照するために保存されるindex値
codebook-scope:同じcodebookを共有するtensor集合。familyより細かい機械的な分割にも使う
codebook-selector:blockごとに複数codebookのどれを使うか選ぶための追加metadata
scale table:scale-formatから生成され、scale-indexを実scale値へ変換する表
scale-index:scale tableを参照するために保存されるindex値
LUT:実装上の表参照機構。量子化仕様上の変換表はcodebookと書く
payload:FP8やFP16など、演算器または後段処理へ渡すための実際の保存・受け渡し値
logical payload bytes:tensorごとに参照されるpayload byte数の合計。共有payloadがある場合は重複分も数える
resident payload bytes:runtime buffer上に実際に保持されるpayload byte数の合計。共有payloadがある場合は重複分を数えない
payload role:.ullm.d内の参照payloadを用途別に選ぶ分類。tensor-index、tensor-scale、tensor-codebook、codebook、passthroughなど
tensor payload bundle:1つのquantized tensorに対応するindex、scale、codebookのpayload集合
host byte helpers:host側のf32/u32値列をruntime bufferへ渡すlittle-endian byte列へ変換する小さい共通処理。readbackしたf32 byte列をhost値へ戻す処理も含む
passthrough payload bundle:1つのpassthrough tensorに対応するraw payloadとdtype、shape、elementsなどのmetadata集合
passthrough f32 data:passthrough payload bundleからBF16またはF32 payloadをchunk読み込みし、host側のf32値列、dtype、shapeとして扱えるようにしたloader API上のデータ
weight registry:runtime bufferへload済みのweight payloadをtensor単位で保持し、後続kernelから参照できるようにするregistry
resident payload:runtime buffer上に保持され、後続処理から再利用できるpayload
codebook pool:複数tensorから参照されるcodebook payloadを一度だけloadし、runtime bufferを共有するためのregistry内管理領域
loaded package handle:package summary、load済みweight registry、registry index一覧をまとめて保持するruntime側のpackage handle
chunked load:大きいpayloadを一定byte数ごとに読み、runtime bufferへ順番に転送するload方式
fused dequant:GEMM/GEMVなどの演算kernel内でcodebook-indexやscaleを実効値へ変換し、そのまま演算へ使う処理
matvec:行列とvectorの積。初期runtimeではmaterialize済みf32 weightとf32 inputを使う最小kernel境界として扱う
materialize:変換後のpayloadや実効値を別bufferへ一度書き出す処理
materialize config:quantized tensor payloadをruntime bufferへmaterializeするときに使うscale format、scale table、group size、tensor scale、element数、出力byte数をまとめたloader API上の設定
materialize smoke:runtime buffer上のquantized payloadをf32などへ展開し、後続kernel境界との接続を確認するsmoke
materialize matvec smoke:quantized payloadをf32へmaterializeした後、同じruntime上でmatvecまで実行して後続演算境界との接続を確認するsmoke
materialize bench:materialize処理をwarmup後に複数回実行し、平均・最小・p50・p95時間を測るbenchmark入口
staging fallback:backend bufferを一度host memoryへ移してCPU処理し、結果をbackend bufferへ戻す一時的な正しさ確認用fallback。性能評価用の実装とは扱わない
block:同じlocal-scaleを共有するraw-valueのまとまり
block-size:1つのblockに含まれるraw-valueの数
KV cache:attention decode用のkey/value cache
block table:sequenceが参照するKV cache block idの配列
KV block allocation:requestやdecode対象token数から必要なKV cache block数を計算し、block tableに使うblock idを割り当てるscheduler側の処理
group:実装上のblockの旧称または変数名。文書ではblockを優先する
superblock:複数のblockをまとめた補正metadataの共有単位
superblock補正:superblock単位に保存する追加metadataでlocal-scaleや実効値を補正する方式
outlier補正:superblock内の大きな残差を少数保存し、dequant後に加算して局所的な誤差を減らす方式
local-scale:数十個のraw-valueごとのscale
unsigned local-scale:符号bitを持たず、常に正値として扱うlocal-scale
signed local-scale:符号bitを持ち、blockごとに正負を選べるlocal-scale。診断・比較用語として使い、仕様候補として扱う場合は明記する
UEaMb:unsigned local-scale用のE/M形式。aはexp bit数、bはmantissa bit数を表す。例: UE4M3、UE5M3
monotonic floor:上位scale形式を評価するとき、下位scale形式の既存解を上位scale tableへ写像してbest-so-farに入れる下限保証
tensor-scale:1つのtensor全体にかかるscale
family-scale:同じfamilyに属するtensorで共有または補正に使うscale
global-scale:local-scaleより大きい単位でのscale。tensor-scaleやfamily-scaleを含む上位概念として使う
family:Q/K/V/OやMLPなど、性質が近いtensorをまとめる分類
control plane:request管理、scheduler、telemetry、APIなど、kernel実行そのもの以外を制御する層
scheduler run state:待機request queue、KV block allocator、active decode requestを同じ状態として保持し、prefill batch選択、KV block allocation、prefill完了、cache_len/generation progress更新を一つのcontrol plane状態として扱う最小状態
scheduler decode batch:prefill完了済みで、まだ生成可能なactive decode requestを安定順序で集めたcontrol plane側のdecode実行候補。各requestについて次に書き込むcache position、decode後のcache_len、残り生成token数を含む
scheduler decode runner:scheduler decode batchから取り出したrequestをruntime paged decode stateへ渡し、成功後にscheduler run stateをadvance_decodeで進める実行境界
scheduler request decode runner:request idごとにruntime decode stateとblock tableを保持し、scheduler decode batchのrequest id、allocation、cache positionを検証してdecode stepを実行するlibrary runner
scheduler multi-request decode runner:2件以上のscheduler decode batchを、1-stepずつ同時にruntime paged decode stateへ与えて検証するrunner境界。成功した場合のみscheduler run stateを一括でadvance_decodeする
multi-request scheduler decode smoke:複数requestを合成し、scheduler multi-request decode runner経由で同一ステップのdecode境界を回すsmoke
multi-request scheduler layer decode smoke:複数requestを合成し、Qwen3 decoder layer request decode runner経由で同一decode batchのattention/block/post RMSNorm/MLP/layer出力とpaged K/V cacheを検証するsmoke
package scheduler layer decode smoke:実packageからmaterializeしたQwen3/Qwen3.5 decoder layer weightを使い、複数requestをSchedulerStateとQwen3 decoder layer request decode runnerでprefill/decode batch実行するsmoke
scheduled paged decode blocks:scheduler run stateを通して得たpaged decode用block tableとcache block数、KV allocator stats、active decode requestのprefill/decode進行値をまとめたsmoke用の結果
runtime:weight、KV cache、backend、kernel呼び出しを管理してmodel executionを実行する層
runtime buffer:runtimeがbackend上またはCPU fallback上に確保するbyte buffer
runtime stream:backend上の非同期copyやkernel実行の順序を束ねる実行stream
host-device-copy:host memoryとruntime bufferの間でpayloadを転送する処理
RMSNorm:root mean square normalization。Qwen系decoder blockでlinear/attention前に使う正規化処理
RMSNorm smoke:runtime buffer上のf32 inputとweightからRMSNormを実行し、CPU/HIP kernel境界を確認するsmoke
package RMSNorm smoke:.ullm.d内の実際のpassthrough RMSNorm weightをf32へ変換し、runtime buffer上の決定的inputと組み合わせてRMSNorm kernel境界を確認するsmoke
SiLU-mul:MLPのgate/up中間値に使うelementwise処理。`silu(gate) * up` を計算する
SiLU-mul smoke:runtime buffer上のf32 gate/upからSiLU-mulを実行し、CPU/HIP kernel境界を確認するsmoke
Sigmoid-mul:Qwen3.5 self-attentionのoutput gateなどに使うelementwise処理。`sigmoid(gate) * input` を計算する
Sigmoid-mul smoke:runtime buffer上のf32 gate/inputからSigmoid-mulを実行し、CPU/HIP kernel境界を確認するsmoke
runtime add smoke:runtime buffer上の2つのf32 tensorを要素ごとに加算し、residual addなどで使うelementwise境界をCPU/HIP kernelで確認するsmoke
runtime RoPE smoke:runtime buffer上のf32 sequence/head tensorへsplit-half RoPEを適用し、CPU/HIP kernel境界を確認するsmoke
runtime causal attention smoke:runtime buffer上のf32 q/k/vからGQA対応のcausal softmax attention value mixを実行し、CPU/HIP kernel境界を確認するsmoke
runtime decode attention smoke:runtime buffer上の1-token f32 qとprefill済みf32 K/V cacheからGQA対応のsoftmax attention value mixを実行し、decode時のKV cache参照境界をCPU/HIP kernelで確認するsmoke。block tableやpaged KV cacheはまだ含まない
runtime paged decode attention smoke:runtime buffer上の1-token f32 q、物理block配置のf32 K/V cache、u32 block tableからGQA対応のdecode attentionを実行し、paged KV cache参照境界をCPU/HIP kernelで確認するsmoke。allocatorやscheduler全体はまだ含まない
runtime paged KV write smoke:runtime buffer上の1-token f32 K/Vとu32 block tableから、指定cache positionに対応する物理paged K/V cache slotへK/Vを書き込む境界をCPU/HIP kernelで確認するsmoke
paged decode state:runtime buffer上のblock table、paged K/V cache、1-token K/V scratch、q/output scratch、written_lenをまとめて保持し、runtime paged KV writeとruntime paged decode attentionを接続するdecoder step用state。cache readbackは論理順ではなく物理cache layoutを返す
paged decode step:1 token分のq/k/vを受け取り、k/vをpaged K/V cacheへ書き込んだ後、その時点のprefix長でpaged decode attentionを実行するdecoder step
paged K/V cache pack:logical順のK/V cacheとblock table、PagedDecodeShapeから、runtime paged decode attentionとcache readbackが使う物理paged K/V cache layoutを作るlibrary utility。prefix decode用にblock tableは必要entry以上を許容する
Qwen3 self attention decode state:Qwen3/Qwen3.5系の狭いself-attention decode用状態。PagedDecodeStateを使い、softmax scaleを保持したうえで1 token分のq/k/vからattention outputを返す
Qwen3 self attention runtime weights:Qwen3/Qwen3.5系の狭いself-attention pathへ渡すq/k/v/o runtime buffer、q/k norm由来head_dim、kv_heads、value_dim、projection shape metadataをまとめるweight set。library runtime APIとしてdecoder.rsに置き、Qwen3 self attention runtime shapeで検証したうえでpackage self-attention系smoke runや後段のdecoder layer stepへ渡す
Qwen3 self attention runtime shape:Qwen3 self attention runtime weightsからhidden、q_heads、kv_heads、head_dim、value_dim、attention_width、q projection layoutを解決し、Qwen3/Qwen3.5 self-attentionの形状整合性を検証するlibrary runtime API
Qwen3 self attention projection sequence:Qwen3/Qwen3.5系self-attentionのhidden入力系列をresident q/k/v projection weightへruntime matvecで適用し、q/k/v projection列をhostへ返すlibrary runtime API。入力系列の生成はsmoke側に残し、projection実行と後段検証を分離する
Qwen3 self attention q projection split:Qwen3/Qwen3.5系self-attentionのq projection出力をqueryと任意のoutput gateへ分けるlibrary runtime API。通常Qwen3ではgateなしのplain layout、Qwen3.5 gated projectionではheadごとにqueryとgateを分離する
Qwen3 headwise RMSNorm:Qwen3/Qwen3.5系self-attentionのq/k head単位RMSNormをruntime kernelで順次実行するlibrary runtime API。host期待値比較はsmoke側に残し、runtime実行と検証を分離する
Qwen3 RoPE:Qwen3/Qwen3.5系self-attentionのq/k head系列へruntime RoPE kernelを適用するlibrary runtime API。host期待値比較はsmoke側に残し、runtime実行と検証を分離する
Qwen3.5 q/k norm RoPE fusion:Qwen3.5 gated self-attention decodeでq projectionのquery/gate split、q/k head単位RMSNorm、q/k RoPEを1つのruntime kernelにまとめるdecode専用境界。attention output gate用のq_gateも同時に出力する
Qwen3.5 q/k norm RoPE paged KV write fusion:Qwen3.5 gated self-attention decodeでq projectionのquery/gate split、q/k head単位RMSNorm、q/k RoPE、paged K/V cache writeを1つのruntime kernelにまとめるdecode専用境界。q_gateとq_ropeを出力し、k_ropeは中間bufferを経由せずpaged K cacheへ直接書き込む
Qwen3 causal attention:Qwen3/Qwen3.5系self-attentionのq/k/v系列からGQA対応のcausal softmax attention value mixをruntime kernelで実行するlibrary runtime API。host期待値比較はsmoke側に残し、runtime実行と検証を分離する
Qwen3 self attention runtime prepared sequence:Qwen3/Qwen3.5系self-attentionのprojected q/k/v sequenceを受け取り、Qwen3 self attention q projection split、Qwen3 headwise RMSNorm、Qwen3 RoPE、Qwen3 causal attentionをruntime APIで連結してhost側中間列を返すlibrary runtime API。host期待値diffと入力系列生成はsmoke側または上位API側に残す
Qwen3 self attention paged decode prepared sequence:Qwen3/Qwen3.5系self-attentionのhidden入力系列、q/k norm weight、RoPE設定、paged K/V cache layoutを受け取り、Qwen3 self attention projection sequence、Qwen3 self attention runtime prepared sequence、paged K/V cache packを連結してpaged decode step検証へ渡せるhost側中間列と物理paged K/V cacheを返すlibrary runtime API
Qwen3 self attention prepared sequence:package self-attention系smoke内でQwen3 self attention paged decode prepared sequenceとhost期待値diffをまとめたprepared sequence。paged decode step検証へ渡すhost側参照データを一箇所に集約する
Qwen3 self attention block step state:Qwen3/Qwen3.5系の狭いself-attention block step用状態。paged K/V write、paged decode attention、Qwen3.5 output gate、o projection、residual addを1 token分ずつ接続する。post RMSNormやMLPは含まない
Qwen3 self attention block sequence:Qwen3/Qwen3.5系の狭いself-attention block sequence実行API。Qwen3 self attention block step stateを内部で作り、q/k/v/gate/residual系列を1 tokenずつ実行してattention output、output gate後入力、o projection出力、residual add後block出力、paged cache readbackをhostへ返す。host期待値diffはsmoke側に残す
Qwen3 decoder layer step state:Qwen3/Qwen3.5系の狭いdecoder layer step用状態。Qwen3 self attention block step stateの出力をpost RMSNorm、MLP gate/up/down、SiLU-mul、final residual addまで1 token分ずつ接続する
Qwen3 post attention runtime weights:Qwen3 decoder layer step stateのpost-attention側へ渡すpost RMSNorm weight、MLP gate/up/down runtime buffer、hidden/intermediate metadataをまとめるlibrary runtime API上のweight set
Qwen3 decoder layer runtime weights:Qwen3 self attention runtime weightsとQwen3 post attention runtime weightsをまとめたlayer-level resident weight set。library runtime APIとしてdecoder.rsに置き、package smokeではこれを先に作ってからself-attention smoke runとdecoder layer runtimeへ分配する
Qwen3 package runtime weight loader:.ullm.d package内のQwen3/Qwen3.5 self-attention/MLP tensorとinput/q/k/post passthrough RMSNorm weightから、Qwen3 self attention runtime weights、Qwen3 post attention runtime weights、Qwen3 decoder layer runtime weights、layer-index単位のpackage decoder layer runtime、複数layerのQwen3 package model runtimeを構築するlibrary側のpackage読み込み境界。低レベルdecoder実行APIにはpackage/loader依存を入れない
Qwen3 package model runtime:複数のQwen3 package decoder layer runtime、層間shape signature、input/q/k/post RMSNorm passthrough metadata、softmax scale、MLP epsilonを保持するlibrary側のpackage model metadata/runtime weight bundle。scheduler、request plan、期待値diff、CLI出力は含まない
Qwen3 package model decode plan:Qwen3 package model runtimeのshape signatureとpaged K/V cache設定からPagedDecodeShape、q/k/v token要素数、attention出力要素数、hidden幅を導出するlibrary側のdecode metadata bundle。scheduler、stack runner構築、prefill/decode loop、期待値diffは含まない
Qwen3 package model stack runner setup:Qwen3 package model runtime、Qwen3 package model decode plan、layerごとのrequest id/block table viewを受け取り、Qwen3 decoder layer stack request decode runnerへresident layer weightとrequest cache stateを登録するlibrary側のrunner setup bridge。scheduler進行、入力slice作成、期待値diffは含まない
Qwen3 package model prefill step run:Qwen3 package model decode planとQwen3 decoder layer decode sequence viewからprefill用1 token inputを作り、Qwen3 decoder layer stack request decode runnerの指定layerでprefill stepを実行するlibrary側の薄い実行境界。prefill完了のSchedulerState更新、smoke期待値diff、decode_steps更新は含まない
Qwen3 package model ready batch run:Qwen3 package model decode planとlayerごとのQwen3 decoder layer decode sequence viewからready decode batch inputを作り、Qwen3 decoder layer stack request decode runnerで全layerを実行してSchedulerStateを1回だけ進めるlibrary側の薄い実行境界。smoke期待値diffやdecode_steps更新は含まない
Qwen3 decoder layer runtime:Qwen3 decoder layer runtime weightsとQwen3 decoder layer step stateをまとめ、step実行時にresident q/k/v/o/post RMSNorm/MLP weightを個別引数へばらさずに使うdecoder.rs上の薄いrunner
Qwen3 decoder layer request decode runner:request idごとにQwen3 decoder layer runtimeとblock tableを保持し、scheduler decode batchのrequest id、allocation、cache positionを検証して、成功後にscheduler run stateをadvance_decodeで進めるlibrary runner
Qwen3 decoder layer stack request decode runner:複数のQwen3 decoder layer request decode runnerをlayer順に保持し、同じscheduler decode batchを全layerでrun_ready_batch_without_advanceした後にSchedulerStateをadvance_decode_batchで1回だけ進めるlibrary runner
Qwen3 decoder layer decode sequence view:request idごとのq/k/v/output gate/residual sequence viewとdecode input layoutから、prefill timestepまたはSchedulerDecodeRequestのcache_positionに対応する1 token分のQwen3 decoder layer decode batch inputを安全に切り出すdecode_runner側の補助API。short sequenceはpanicではなくエラーにする
Qwen3 scheduler layer decode state:smoke-localのSchedulerLayerDecodeRun内で、request id、prompt/decode token数、block table、q/k/v/output gate/residual sequence、decode_stepsをまとめる入力・進行状態。期待値系列や最大絶対誤差は含まない
Qwen3 scheduler layer decode smoke checks:smoke-localのSchedulerLayerDecodeRunから分離した期待値系列と最大絶対誤差の保持領域。request id、block table、q/k/v/output gate/residual sequence、decode_stepsなどの入力・進行状態とは分けて扱う
Qwen3 decoder layer sequence:Qwen3/Qwen3.5系の狭いdecoder layer sequence実行API。Qwen3 decoder layer runtimeを内部で作り、q/k/v/gate/residual系列を1 tokenずつ実行してattention出力、output gate後入力、o projection出力、self-attention block出力、post RMSNorm出力、MLP出力、final residual add後のlayer出力、paged cache readbackをhostへ返す。host期待値diffはsmoke側に残す
runtime KV paged decode smoke:Rust側のKvBlockAllocatorで非連続block tableを作り、そのblock tableとhost側で検証用にpackした物理paged K/V cacheをruntime paged decode attentionへ渡すsmoke。scheduler管理とruntime kernel境界の最小接続を確認する
depthwise conv1d:channelごとに独立した1次元畳み込み。linear attentionのconv1d.weightでは入力系列の各channelに同じchannelのkernelだけを適用する
runtime depthwise conv1d smoke:runtime buffer上のf32系列とf32 weightからcausal depthwise conv1dを実行し、CPU/HIP kernel境界を確認するsmoke
MLP smoke:RMSNorm、gate/up/down matvec、SiLU-mulを小さい固定tensorで接続し、decoder MLP相当のruntime workflowを確認するsmoke
package MLP smoke:.ullm.d内の実際のgate/up/down quantized tensorをmaterializeし、matvecとSiLU-mulでMLP相当のruntime workflowを確認するsmoke
package RMSNorm MLP smoke:.ullm.d内の実際のpassthrough RMSNorm weightとgate/up/down quantized tensorを使い、RMSNormからMLPまでをruntime上で接続するsmoke
package MLP block smoke:.ullm.d内の実際のpost_attention_layernorm.weightとMLP quantized tensorを使い、RMSNormからMLP workflow、residual addまでを接続するsmoke。attentionやdecoder block全体の計算ではない
package linear attention projection smoke:.ullm.d内の実際のlinear_attn projection quantized tensorをmaterializeし、共通の決定的inputからmatvecまで接続するsmoke。linear attention全体の計算ではない
package self attention projection smoke:.ullm.d内の実際のself_attn q/k/v/o projection quantized tensorをmaterializeし、共通の決定的inputからmatvecまで接続するsmoke。通常self-attention全体の計算ではない
package self attention QK norm smoke:.ullm.d内の実際のself_attn q/k projection quantized tensorとq_norm/k_norm passthrough tensorを使い、projection出力をhead単位でRMSNormへ接続するsmoke。RoPE、attention score、KV cacheはまだ含まない
package self attention RoPE smoke:.ullm.d内の実際のself_attn q/k projection quantized tensorとq_norm/k_norm passthrough tensorを使い、projection、head単位RMSNorm、runtime RoPEまで接続するsmoke。attention score、KV cacheはまだ含まない
package self attention attention smoke:.ullm.d内の実際のself_attn q/k/v projection quantized tensorとq_norm/k_norm passthrough tensorを使い、projection、head単位RMSNorm、runtime RoPE、runtime causal attention value mixまで接続するsmoke。Qwen3.5のgated q projectionではper-headのquery/gate splitを行い、queryだけをattentionへ渡す。o_proj、residual、KV cacheはまだ含まない
package self attention decode smoke:.ullm.d内の実際のself_attn q/k/v projection quantized tensorとq_norm/k_norm passthrough tensorを使い、prefill済みK/V cache相当の系列と最後の1 token queryをruntime decode attentionへ渡すsmoke。full causal attentionの最後のtokenと比較し、同じK/V系列をscheduler run state由来のblock tableに従ってruntime paged KV writeで物理paged K/V cacheへ書き込み、そのruntime bufferをruntime paged decode attentionへ渡す経路も検証する
package self attention block smoke:.ullm.d内の実際のself_attn q/k/v/o projection quantized tensorとq_norm/k_norm passthrough tensorをQwen3 self attention runtime weightsへまとめ、Qwen3 self attention prepared sequenceを作ったうえで、Qwen3 self attention block sequenceによりruntime paged KV write、paged decode attention、runtime Sigmoid-mulによるQwen3.5 output gate、o projection、residual addまで接続するsmoke。host期待値diffはsmoke側に残し、MLPはまだ含まない
package self attention MLP block smoke:.ullm.d内の実際のself_attn q/k/v/o projection quantized tensor、q_norm/k_norm、post_attention_layernorm.weight、MLP tensorからQwen3 decoder layer runtime weightsを先に作り、Qwen3 self attention paged decode prepared sequenceを経由してQwen3.5 self-attention block出力をpost RMSNorm、MLP、final residual addまで接続するlayer-level partial decoder smoke。full causal attentionを参照として残しつつ、Qwen3 decoder layer sequenceでruntime paged KV write、paged decode attention、output gate、o projection、residual add、post RMSNorm、MLP、final residual addを1 tokenずつ実行する
package self attention MLP block scheduler smoke:.ullm.d内の実際のself_attn q/k/v/o projection quantized tensor、q_norm/k_norm、post_attention_layernorm.weight、MLP tensorからQwen3 decoder layer runtime weightsを作り、複数requestのQwen3 self attention prepared sequenceをSchedulerStateとQwen3 decoder layer request decode runnerでprefill/decode batch実行するpackage scheduler layer decode smoke
package model loop smoke model:package self attention MLP block model loop smokeで使うmodel runtime bundle。現在はlibrary側のQwen3 package model runtimeを使い、smoke-localにはscheduler、request plan、期待値diff、stack runner構築、CLI出力だけを残す
package model loop request plan:package self attention MLP block model loop smoke用にrequest列、SchedulerState、block table、total token数、初期residual sequence、paged block設定をまとめるsmoke-local request scheduling bundle
package model loop layer list:package self attention MLP block model loop smokeで順に実行するdecoder layer index列。CLIでは`LAYER_INDEX,...`または`FIRST_LAYER_INDEX SECOND_LAYER_INDEX[,...]`で指定し、既定は`[3, 7]`とする
package model loop layer run plan:package self attention MLP block model loop smoke用にlayerごとのQwen3 self attention prepared sequence、Qwen3 decoder layer expected sequence、SchedulerLayerDecodeRun列、projection layout metadata、prepared/runtime diff集計をまとめるsmoke-local layer execution plan
package model loop execution plan:package self attention MLP block model loop smoke用にQwen3 package model decode plan、Qwen3 package model stack runner setup呼び出し、Qwen3 decoder layer decode sequence viewによるprefill/ready batch input切り出し、prefill/decode batch実行、SchedulerState進行、paged K/V cache readback検証をまとめるsmoke-local execution orchestration plan
package model loop scheduler decode loop:package self attention MLP block model loop smokeでSchedulerStateからready decode batchを空になるまで取得し、各batchを全decoder layer stackへ通してからSchedulerStateを進めるdecode loop。固定のrequest id列ではなく、scheduler run stateの進行からbatch数とbatch sizeを決める
package model loop smoke run:package self attention MLP block model loop smoke用にpackage model loop smoke model、request plan、layer run plan、execution plan、execution summary、出力用設定を束ねるsmoke-local run object。RuntimeContext、RuntimeStream、stack runnerは保持しない
package self attention MLP block model loop smoke:.ullm.d内の複数self_attn decoder layerを順にmaterializeし、前layerのlayer outputを次layerのresidual inputとして使い、複数requestをQwen3 decoder layer stack request decode runnerで実行して全layer完了後にSchedulerStateを1 token分だけadvance_decode_batchするsmoke
package linear attention QKV norm smoke:.ullm.d内の実際のlinear_attn.in_proj_qkv quantized tensorとlinear_attn.norm.weight passthrough tensorを使い、QKV projection出力の先頭128要素にRMSNormを適用するsmoke。linear attention全体の計算ではない
package linear attention conv1d smoke:.ullm.d内の実際のlinear_attn.in_proj_qkv quantized tensorから複数timestepのQKV出力を作り、linear_attn.conv1d.weight passthrough tensorでcausal depthwise conv1dを適用するsmoke。linear attention全体の計算ではない
package linear attention recurrent smoke:.ullm.d内の実際のlinear_attn.in_proj_qkv / conv1d / in_proj_a,b / A_log / dt_biasを使い、q/k/v split と gate/beta を recurrent境界へ接続するsmoke。linear attention全体の計算ではない
package linear attention post smoke:.ullm.d内の実際のlinear_attn.in_proj_z / norm / out_projを使い、post-recurrentのgated RMSNormからout projectionまでを接続するsmoke。linear attention全体の計算ではない
package linear attention block smoke:.ullm.d内の実際のinput_layernorm.weightとlinear_attn系tensorを使い、input RMSNormからlinear attention workflow、residual addまでを接続するsmoke。MLPやdecoder block全体の計算ではない
package linear attention workflow smoke:.ullm.d内の実際のlinear_attn.in_proj_qkv / conv1d / in_proj_a,b / A_log / dt_bias / in_proj_z / norm / out_projを使い、recurrent境界からpost-recurrent gated RMSNormとout projectionまでを接続するsmoke。linear attention全体の計算に近いが、まだ通常decoder layer全体ではない
package linear attention auxiliary smoke:.ullm.d内の実際のlinear_attn補助passthrough tensorをf32へ変換し、runtime bufferへの転送とreadbackを検証するsmoke。linear attention全体の計算ではない
package linear attention MLP block smoke:.ullm.d内の実際のinput_layernorm.weight、linear_attn系tensor、post_attention_layernorm.weight、MLP tensorを使い、linear attention block出力をMLP blockのresidual inputに渡すlayer-level partial decoder smoke。既定は1 tokenで、任意のsequence長では各tokenのMLPまで接続する。通常attention層やKV cache管理はまだ含まない
linear attention gate beta:linear attentionでin_proj_a/bの出力、A_log、dt_biasからgateとbetaを作る境界処理
runtime linear attention gate beta smoke:runtime上でa/b、A_log、dt_biasからgate/betaを計算し、決定的入力で境界動作を確認するsmoke。linear attention全体の計算ではない
package linear attention gate beta smoke:.ullm.d内の実際のlinear_attn.in_proj_a/b quantized tensorとA_log/dt_bias passthrough tensorを使い、runtimeでgate/beta境界を検証するsmoke。linear attention全体の計算ではない
linear attention recurrent:linear attentionの逐次state更新処理。uLLMではq/kをkey head layout、v/gate/beta/output/stateをvalue head layoutで扱う
linear attention recurrent prefill kernel:linear attention recurrentをprompt token列に対して実行するHIP kernel経路。timestep方向の依存は残しつつ、value head/value dimensionごとにblockを分け、key_dim方向をparallel reductionする
runtime linear attention recurrent smoke:runtime上で小型f32 q/k/v/gate/beta/stateからlinear attention recurrent境界を計算し、更新後stateとoutputを検証するsmoke。linear attention全体の計算ではない
package linear attention recurrent batch smoke:.ullm.d内の実際のinput RMSNorm、linear_attn qkv/a/b projection、conv1d、gate/beta、recurrentをtoken batchで接続し、prefill向けlinear attention前半のdevice-resident実行を検証・計測するsmoke
package linear attention post batch smoke:.ullm.d内の実際のlinear_attn z/out projectionとnormを使い、token batchのpost-recurrent RMSNorm/SiLU、out projection、residual addをdevice-resident実行で検証・計測するsmoke
package linear attention attention batch smoke:.ullm.d内のlinear_attn qkv/z/a/b/conv/A_log/dt_bias/norm/outを使い、input RMSNormからrecurrent、post-recurrent処理、out projection、residual addまでを同一token batchで接続するprefill向けpartial attention smoke
package linear attention MLP batch smoke:.ullm.d内のpost_attention_layernorm.weightとMLP gate/up/down projectionを使い、token batchのMLP側RMSNorm、SiLU積、down projection、residual addをdevice-resident実行で検証・計測するsmoke
package linear attention layer batch smoke:.ullm.d内のlinear_attn attention側とMLP側を同一token batch・同一buffer列で接続し、linear attention decoder layer相当のprefill partial layerをdevice-resident実行で検証・計測するsmoke
package self attention QKV RoPE batch smoke:.ullm.d内のself_attn q/k/v projection、input RMSNorm、Qwen3.5 QK norm RoPE batchをtoken batchとして接続し、self-attention prefill前半のdevice-resident実行を検証・計測するsmoke
package self attention attention batch smoke:.ullm.d内のself_attn q/k/v projection、input RMSNorm、Qwen3.5 QK norm RoPE batch、causal attentionをtoken batchとして接続し、self-attention prefill attention側のdevice-resident実行を検証・計測するsmoke
package self attention block batch smoke:.ullm.d内のself_attn q/k/v/o projection、input RMSNorm、Qwen3.5 QK norm RoPE batch、causal attention、output gate、o projection、residual addをtoken batchとして接続し、self-attention block側のdevice-resident実行を検証・計測するsmoke。MLPは含まない
package self attention layer batch smoke:.ullm.d内のself_attn block batchにpost_attention_layernorm.weightとMLP gate/up/down projectionを接続し、post RMSNorm、SiLU積、down projection、final residual addまでを同一token batchで実行するself-attention decoder layer partial smoke
Qwen3.5 QK norm RoPE batch:Qwen3.5 self-attention prefill向けに、token-majorのq_proj/k_proj出力からQ gate分離、headwise RMSNorm、RoPEを複数token分まとめて実行するruntime primitive
runtime causal attention batch:prefill向けのcausal attentionをq/k/v/outputの`[batch, sequence, head, dim]` layoutで複数request分まとめて実行するruntime primitive
runtime causal attention batch smoke:R9700などのruntime backend上でbatched causal attention primitiveを実行し、cold prefillのtotal input token/s、attention pair/s、sampled output guardを記録するcomponent smoke
online softmax causal attention:causal attentionでscore列を全て保存せず、source tokenを走査しながら現在の最大score、softmax denominator、weighted valueを更新するkernel方針。uLLMでは通常shapeでq/k score dot-productの再計算を減らすために使う
online softmax cached prefix attention:cached prefix attentionで長いprefixを走査しながら最大score、denominator、weighted valueを更新し、`M x L` prefill時のscore再計算を減らすkernel方針
head-parallel decode attention:decode attentionで1つのq headを1つのthread blockに割り当て、head_dim方向のdot積reductionとvalue_dim方向のweighted valueを同じblock内で計算するkernel方針
split-source paged decode attention:paged decode attentionのsource token列を複数tileへ分割し、q headとtileごとのonline softmax partial stateをworkspaceへ書いた後、tile順に数値安定なmergeを行う明示的runtime primitive
Radeon fallback backend:vLLM/ROCmでInstinct向け最適backendが使えない場合に、Radeon GPUで使うROCM_ATTNやTriton系のfallback backend。FP8 native比較とは別行で記録する
throughput_gen:requests x output length / elapsed timeで計算される生成token throughput。elapsed timeがprefill込みの場合はdecode-only throughputと直接比較しない
HIPRTC:HIP runtime compilation。通常C++でbuildしたruntimeからHIP kernel sourceを実行時compileしてmodule launchする仕組み
JIT kernel:実行時にcompileしてloadするkernel。uLLMではHIPRTC経由のAQ4 materialize kernelなどを指す
kernel-required smoke:staging fallbackを禁止し、実kernel経路だけで成功することを確認するsmoke
backend:HIP、CPU、CUDA、CANN、JAX/TPUなど、runtimeが実際に演算へ使う実装単位
backend capability:backendごとに異なる対応機能や推奨値。block-size、dtype、kernel variantなどを含む
telemetry:実行中に記録する性能・メモリ・allocator状態などの測定値
golden tensor fixture:参照実装から固定token id、position、layer indexに対するhidden stateなどを保存し、uLLM側のlayer単位またはlogits単位の正しさ確認に使う小さい検証用データ
package layer golden smoke:.ullm.d内の実package weightを使って指定Qwen3 decoder layerを実行し、golden tensor fixture内の参照hidden stateとMSE、平均絶対誤差、最大絶対誤差、cosine similarityで比較するsmoke
golden prefix fixture:参照実装から固定token id、position、連続decoder layer rangeに対する初期hidden stateと各layer後のhidden stateを保存し、uLLM側の層間residual受け渡しと誤差蓄積を確認する検証用データ
package golden prefix smoke:.ullm.d内の実package weightを使って連続Qwen3 decoder layerを順に実行し、golden prefix fixture内の各layer後hidden stateとMSE、平均絶対誤差、最大絶対誤差、cosine similarityで比較するsmoke
effective RMSNorm weight:Qwen3.5系で保存値をそのままscaleとして使わず、参照実装と同じく`1 + weight`へ変換したRMSNorm用weight。Qwen3系の通常weightやlinear_attn.norm.weightは直接weightとして扱う
hot input vector:golden prefixの最大output差分が出たtoken/hidden coordinateについて、そのhiddenを生む直前のattention projection inputやMLP activationなどの入力vector統計を記録した診断情報
causal Conv1d weight order:PyTorchの`Conv1d(padding=kernel_size-1)`で現在tokenに対応するkernel位置が最後のweight要素になる順序。linear attentionのdepthwise conv1dで参照実装とruntimeの境界を合わせるために使う
Qwen3.5 gated RMSNorm:linear attentionのrecurrent出力を`head_v_dim`単位にreshapeし、head内でRMSNormした後に同じhead単位の`F.silu(z)`を掛ける正規化境界。参照実装では`linear_attn.norm`の出力がそのまま`out_proj`入力になる
projection row-dot check:full-referenceの入力vectorに対してsource weight rowとpackage AQ4復元rowのdot積を比較し、projection単体の量子化寄与を測る診断
self-attention module trace:full_attention decoder layerをgolden fixtureのbefore tensorから単体再生し、self_attn q/k/v/o projection、q/k norm、output gate、post RMSNorm、MLP down projectionの中間vectorとrow-dot誤差を記録する診断
self-attention propagation diagnostic:package AQ4から復元したq/k/v projectionをfull-referenceと同じq/k norm、RoPE、causal attention、output gateへ通し、`o_proj`入力と指定hidden rowの出力寄与まで量子化誤差がどう残るかを測る診断
sampled feature:hot input vector内で、あるstageのtop absolute feature indexを他stageでも同じfeature indexとして記録する診断点。stageごとのtop feature集合が一致しない場合でも、同じ内部featureの値と誤差を追跡できる
head RMS diagnostic:Qwen3.5 gated RMSNormのようにhead_v_dim単位で正規化する境界について、sampled featureが属するhead内のRMSやmax_absをpackage/full-referenceで比較する診断
linear attention Conv1d activation:Qwen3.5 linear attentionではdepthwise Conv1d出力にSiLUを適用してからq/k/v splitとrecurrentへ渡す境界。uLLMのdepthwise_conv1d_f32は純粋な畳み込みkernelとして残し、参照実装に合わせるcaller側でpost-SiLU値を使う
self-attention input RMSNorm:Qwen3/Qwen3.5 decoder layerのself-attentionで、層入力をinput_layernormで正規化してからq/k/v projectionへ渡す境界。residual addには正規化前の層入力を使うため、projection入力とresidual入力を分けて扱う
row-scale overrides:package manifestの任意metadataとして、量子化済みmatrixをAQ4 materializeした直後に指定rowへ正の有限scaleを掛ける補正情報。full tensor name、row index、scale、任意sourceを持ち、元のquantized payloadは変更しない
manifest row-scale compensation:row-scale overridesをloader/materialize経路で適用するprototype補正。smoke-only CLI overrideではなくpackage metadataから有効化されるため、通常のpackage materialize利用者にも同じ補正済みruntime matrixを返す
cached prefix flash2 attention:cached prefix prefill向けに、長いprefixを固定token tileに分け、各tileのscoreをshared memoryに保持しながらonline softmaxでmax、denominator、weighted valueを更新するFlashAttention2-style runtime kernel。現在の初期実装はR9700/RDNA4のFP8 E4M3 K/V cache評価向けで、FP8値とscaleをkernel内で復号して使う
manifest self-attention layer list:.ullm.d package manifest内のself_attn q_norm/k_norm passthrough tensorから抽出したself-attention layer index列。mixed-attention full packageでself-attention層だけをmodel-loop中間guardとして実行するために使い、full mixed-attention layer orderとは扱わない
package layer kind inventory:.ullm.d package manifestからsupported decoder layer indexとlayer kindを抽出し、full mixed-attention runnerの実装対象layer orderを固定するinventory。throughput rowではなく、self-attentionとlinear-attentionを混在実行するための前提情報として扱う
linear-attention request state owner:linear-attention層のrecurrent stateとcausal Conv1d historyをrequest idごとに分離して保持するrunner-side owner。full mixed-attention real-batch runnerでlinear-attention層をscheduler request単位に進めるための境界
package linear-attention request state smoke:PackageLinearAttnResidentStepBatchLayerを実packageのlinear-attention層で実行し、interleaved request実行の出力がrequestごとの単体resident layer実行と一致することを確認するsmoke。throughput rowではなく、full mixed-attention runnerへ接続する前のstate分離guardとして扱う
self-attention request state owner:self-attention層のpaged KV cache、written_len、block tableをrequest idごとに分離して保持するrunner-side owner。full mixed-attention real-batch runnerでself-attention層とlinear-attention層を同じrequest-id dispatch形へ揃えるための境界
mixed request-state layer enum:linear-attention request state ownerとself-attention request state ownerを同じlayer列に並べ、request idごとにdevice-resident residualを受け渡すrunner-side dispatch境界。throughput rowではなく、full mixed-attention runnerへ進む前のstate分離guardとして扱う
package mixed request-state smoke:package-token-ids-mixed-request-state-smokeで実packageのlinear-attention層とself-attention層を混在実行し、token ID embedding入力、request-state dispatch、final RMSNorm、lm_head top1 guardを確認するsmoke。現在はrequest_state_interleavedであり、real batch throughput比較には使わない
SQ8 canonical artifact:source checkpointのFP8 weight payloadとblock scale payloadを再量子化せずbyte-exactに保持するSQ8_0 artifact。GPU固有prepackとは分離し、source tensor名、shape、dtype、block shape、復元式、payload hashを固定する
SQ8 block-2D scale:weightの行blockと列blockの2軸で共有するscale layout。weight座標`[row,col]`には`scale[row/block_rows,col/block_cols]`をdequant multiplierとして掛ける
SQ8 reference W8A16 profile:canonical SQ8 artifactのFP8 weightを固定上限のbufferで読み、compact BF16 block-2D scaleを使い、F64 accumulatorからF32出力を作る正しさ確認用profile。payload全体やF32 weight matrixは展開せず、R9700向けoptimized性能を意味しない
SQ8 correctness report:canonical artifact hash、tensor名、F32-LE input hash、reference implementation profile、execution path、fallback状態、nonfinite数、MSE、最大絶対誤差、relative L2、cosine similarity、固定閾値の判定結果を保持する型付きJSON report
mixed request-state manifest smoke:package-token-ids-mixed-request-state-smokeをmanifest-allで実行し、full mixed-attention packageのlayer order全体をrequest-state dispatchで通すsmoke。throughput rowではなく、shared-weight real batch runnerへ進む前のfull-order guardとして扱う
slot AQ4 payload registry sharing:同一resident batch layer内の複数request slotが同じWeightRegistryを使い、同名AQ4 tensorのindex/scale/codebook runtime bufferを共有する中間段階。完全なshared resident weightsではなく、scale_values、passthrough weights、workspace、state/cacheはまだslotごとに残る
