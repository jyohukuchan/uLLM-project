# Qwen3.5 AQ4 P2 path oracle

- GPU evidence remains a single sanctioned run; no model rerun was performed.
- Detached `path-oracle-binary` and `worker-binary` copies are mode `0755`, `nlink=1`, and SHA-bound to their active execution paths.
- The independent attestation validator accepts corrected v2 package-only identity and replay alignment, while exact greedy/top-k and threshold-policy blockers remain.
